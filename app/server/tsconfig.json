{
  "compilerOptions": {
    /* 1. Настройка компиляции для ES Modules в Node.js */
    "target": "esnext",              /* Целевая версия ECMAScript для скомпилированного кода */
    "module": "NodeNext",            /* Стратегия генерации кода модуля (NodeNext соответствует 'type': 'module') */
    "moduleResolution": "NodeNext",  /* Стратегия поиска модулей (обязательно NodeNext при module: NodeNext) */

    /* 2. Настройка исходных и выходных папок */
    "rootDir": "./src",              /* Корневая папка с исходным кодом TypeScript (.ts файлы) */
    "outDir": "./dist",              /* Папка, куда будет помещен скомпилированный JavaScript (.js файлы) */
    "sourceMap": true,               /* Генерация файлов source maps для отладки */
    
    /* 3. Строгость и правила */
    "strict": true,                  /* Включает все строгие проверки типов (рекомендуется) */
    "forceConsistentCasingInFileNames": true, /* Принудительная проверка регистра имён файлов */
    "skipLibCheck": true,            /* Пропускает проверку библиотечных файлов объявлений для ускорения */

    /* 4. Настройки совместимости */
    "esModuleInterop": true,         /* Обеспечивает совместимость импорта CommonJS как ES Modules (для express) */
    "resolveJsonModule": true,       /* Разрешает импорт .json файлов */
    "allowSyntheticDefaultImports": true /* Разрешает импорт по умолчанию из модулей без экспорта по умолчанию */
  },
  
  /* 5. Какие файлы компилировать */
  "include": [
    "src/**/*"                       /* Включает все файлы во всех подпапках 'src' */
  ],
  
  /* 6. Какие файлы исключить */
  "exclude": [
    "node_modules",                  /* Исключаем зависимости */
    "dist"                           /* Исключаем папку с выходными файлами */
  ]
}